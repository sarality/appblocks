<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Description: Common Layout for all classes that use a Navigation Drawer.

    Author: abhideep@dothat.in (Abhideep Singh)
-->
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The main content view -->
    <include layout="@layout/navigation_drawer_content"/>

    <!-- The navigation drawer list that will be displayed -->
    <ListView android:id="@+id/drawer_list"
        android:layout_width="180dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:divider="@android:color/white"
        android:dividerHeight="0dp"
        android:background="#111"/>
</android.support.v4.widget.DrawerLayout>