package com.sarality.app.view.list;

/**
 * Base implementation for ListViewRowRenderer.
 *
 * @param <H> Type of ViewHolder
 * @param <T> Type of data for the
 * @author abhideep@ (Abhideep Singh)
 */
public abstract class BaseListViewRowRenderer<H, T> extends ViewHolderBasedRenderer<H, T>
    implements ListViewRowRenderer<T> {

  // All methods are abstract
}
